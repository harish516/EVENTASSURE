import { CustomerModel } from './customer.model';

/** The list object to hold complaint objects. */
export class CustomerListModel {

    /** Array to hold the list of complaint objects. */
    list: CustomerModel[] = [];

}
