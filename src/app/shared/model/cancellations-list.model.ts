import { CancellationModel } from './cancellation.model';

/** The list object to hold cancellation objects. */
export class CancellationListModel {

    /** Array to hold the list of cancellation objects. */
    list: CancellationModel[] = [];

}
